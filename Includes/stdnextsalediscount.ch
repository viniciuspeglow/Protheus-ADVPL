#ifdef SPANISH
	#define STR0001 "Sin conexion. La NCC se genera cuando la conexion se restablezca"
#else
	#ifdef ENGLISH
		#define STR0001 "Without Connection NCC will be generated when the connection is reestablished"
	#else
		#define STR0001 "Sem conexão. A NCC será gerada quando a conexão for reestabelecida"
	#endif
#endif
